class urls {
    static origin = 'http://localhost:8000/api/v1/';
    static posts = this.origin + 'posts';
    static newPost = this.origin + 'posts/store';
    static musics = this.origin + 'musics';
}

export default urls;